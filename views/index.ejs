<%- include header %>

<% if(!locals.user) { %>
	<div class="hero-unit">
		<h1>欢迎来到Napblog</h1>
		<p>Napblog是一个基于Node.js的微博系统。</p>
		<p>
			<a class="btn btn-primary btn-large" href="/login">登录</a>
			<a class="btn btn-large" href="/reg">立即注册</a>
		</p>
	</div>
<%  } else { %>
	<%- partial('say')  %>
<% } %>


<%- include footer %>